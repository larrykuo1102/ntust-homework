# Iperfer



```bash
# server
python Iperfer.py -s -p 1025

#client
python Iperfer.py -c -p 1025 -h 127.0.0.1 -t 5

##
# if you don't assign port it will set by default 3000
# if you don't assign host it will set by default 127.0.0.1
```
